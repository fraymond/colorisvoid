---
description: 将 AI 新闻字幕请求路由到字幕工作流
alwaysApply: true
---

# AI 新闻字幕工作流

当用户提到 `字幕`、`SRT`、`口播字幕`、`重跑字幕`、`按文本对字幕`、`英文不要拆开`、`数字小数不要拆`、`字幕不要两行`，或者给出 AI 新闻视频路径并明确要求优化字幕时，主动优先使用 `ai-news-subtitles` skill。

把这类请求理解成“先确认文本来源，再重做字幕”。如果用户提供了文本、网页正文来源、同名 `.txt`，或明确说“按我给的文本来”，就把该文本视为权威源，不要退回默认抓取逻辑。

默认提醒自己检查这些点：
- 输入视频路径是否明确
- 文本来源是否明确，且优先级是否正确
- 是否要求单行字幕
- 英文单词和英文空格是否要严格保留
- 小数和百分比是否需要整体保护
- 是否要求重跑 `.srt` 和烧字幕成片
