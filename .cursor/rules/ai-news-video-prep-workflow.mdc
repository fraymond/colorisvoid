---
description: 将 AI 新闻视频请求路由到视频准备工作流
alwaysApply: true
---

# AI 新闻视频准备工作流

当用户提到 `AI_news_raw`、`AI_news_processed`、本地 AI 新闻视频路径，或者要求“加字幕”“做标题卡”“加日期”“做成可发小红书版本”时，主动优先使用 `ai-news-video-prep` skill。

把这类请求理解成“先准备视频，再决定是否发布”。如果用户同时提到发小红书，也要先确认视频是否已经包含最终标题卡和所需字幕；没有的话，先走视频准备流程。

默认提醒自己检查这些点：
- 输入视频路径是否明确
- 字幕是否已经烧入
- 标题卡是否需要按默认样式生成
- 日期按 UTC 还是本地时间
- 最终输出是否需要保留为新文件而不是覆盖源文件
