---
description: 将 AI_news_post 视频路由到小红书发布工作流
alwaysApply: true
---

# 小红书发布工作流

当用户提到 `AI_news_post`、`~/Documents/AI_news_post`，或者要求把本地视频发到小红书时，主动优先使用 `xiaohongshu-video-publish` skill。

把“视频丢进 `AI_news_post` 了”理解成对话触发条件，而不是字面意义上的文件系统监听器。只要本地视频路径已经给出，或语义上已经很明确，就直接进入小红书发布流程，不要再等额外提示。

开始发布前，先确认这些前提：
- 小红书登录态还在浏览器里活着
- 系统文件选择框或页面 DOM 至少有一个可控

如果前提不满足，先直接告诉用户：
- `小红书登录态要在浏览器里还活着`
- `系统文件选择框或页面 DOM 至少要能控制一个，不然最后一步会卡住`
